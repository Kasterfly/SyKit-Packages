[
    {
        "action": "append",
        "content": "\n# Supabase (sykit.supabase): REST credentials for db.connect(\"supabase:...\").\n# Use the service role key; it must never reach a browser.\n# SYKIT_SUPABASE_URL=\n# SYKIT_SUPABASE_KEY=\n"
    }
]
