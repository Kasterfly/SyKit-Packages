[
    {
        "action": "append",
        "content": "\n# Database (sykit.db): sqlite file used by db.connect() when no path is\n# given. Relative paths resolve from the app's working directory.\n# SYKIT_DB_PATH=\n"
    }
]
